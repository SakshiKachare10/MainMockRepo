let books = document.getElementById("books");
form.addEventListener("submit",function()
{
    let title = form.name.value;
    let author = form.name.value;
    let category = form.name.value;

    console.log({title,author,category})
})
